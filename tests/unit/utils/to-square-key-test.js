import toSquareKey from '../../../utils/to-square-key';
import { module, test } from 'qunit';

module('Unit | Utility | to square key');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toSquareKey();
  assert.ok(result);
});
