import keyMap from '../../../utils/key-map';
import { module, test } from 'qunit';

module('Unit | Utility | key map');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = keyMap();
  assert.ok(result);
});
