import tetrominos from '../../../utils/tetrominos';
import { module, test } from 'qunit';

module('Unit | Utility | tetrominos');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tetrominos();
  assert.ok(result);
});
