import rotations from '../../../utils/rotations';
import { module, test } from 'qunit';

module('Unit | Utility | rotations');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = rotations();
  assert.ok(result);
});
