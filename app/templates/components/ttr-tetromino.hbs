<rect
  x="0px"
  y="0px"
  width="{{box.w}}px"
  height="{{box.h}}px"
  style="fill:none;"
  />
{{#each positions as |pos|}}
  <rect
    x="{{pos.x}}px"
    y="{{pos.y}}px"
    width="{{scale}}px"
    height="{{scale}}px"
    class="ttr-tetromino__square--{{type}}"
  />
{{/each}}
